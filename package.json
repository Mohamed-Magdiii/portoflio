{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "backend": "npm --prefix backend run dev",
    "deploy:frontend": "cd frontend && npx --yes vercel --prod --yes",
    "deploy:backend": "cd backend && npx --yes vercel --prod --yes"
  }
}
